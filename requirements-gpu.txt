# GPU profile (run on a CUDA instance, e.g. RunPod / Lambda).
# Boltz-2 is the deferred PRIMARY affinity scorer. It is installed STANDALONE,
# NOT alongside requirements-core.txt: Boltz pins its own consistent scientific
# stack (numpy<2, scipy==1.13.1, scikit-learn==1.6.1, pandas>=2.2.2,
# rdkit>=2024.3.2, pyyaml==6.0.2, torch>=2.2), and core's scikit-learn==1.5.1 pin
# makes pip resolution impossible. The only harness dep Boltz does NOT pull is
# matplotlib (for the calibration plot), so we add it here.
# On a GPU box, ensure a CUDA torch build if torch.cuda.is_available() is False
# (see README): pip install --index-url https://download.pytorch.org/whl/cu124 torch
boltz==2.2.1
matplotlib>=3.8
# Boltz's triangle layers need NVIDIA cuEquivariance kernels or they crash
# (ModuleNotFoundError: cuequivariance_torch). cu12 ops cover CUDA 12.x drivers.
# (If unavailable on your box, set use_kernels: false in target.yaml for the
# pure-torch fallback.)
cuequivariance-torch
cuequivariance-ops-torch-cu12
